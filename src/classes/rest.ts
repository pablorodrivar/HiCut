
export class Rest{

    public static http(
            method:String,
            url:String,
            body:string[],
            headers:string[],
            callback:(data : any) => void
        ) {
        
    }
}